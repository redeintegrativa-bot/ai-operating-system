# Architecture Decisions

> Records of architectural decisions, patterns, and design choices.

## Metadata

- **Generated**: `{{timestamp}}`
- **Version**: `{{version}}`

---

## 1. Architecture Overview

### 1.1 System Architecture Type

`{{arch_type}}` (e.g., microservices, monolith, event-driven, layered)

### 1.2 Core Design Principles

1. `{{principle_1}}`
2. `{{principle_2}}`
3. `{{principle_3}}`

### 1.3 Technology Stack

| Layer | Technology | Rationale |
|-------|-----------|-----------|
| Frontend | `{{tech}}` | `{{reason}}` |
| Backend | `{{tech}}` | `{{reason}}` |
| Database | `{{tech}}` | `{{reason}}` |
| Infrastructure | `{{tech}}` | `{{reason}}` |

---

## 2. Architectural Decision Records (ADR)

### ADR-001: `{{title}}`

- **Date**: `{{date}}`
- **Status**: `{{status}}` (proposed | accepted | deprecated | superseded)
- **Context**: `{{context}}`
- **Decision**: `{{decision}}`
- **Consequences**:
  - Positive: `{{positive}}`
  - Negative: `{{negative}}`
  - Neutral: `{{neutral}}`

### ADR-002: `{{title}}`

- **Date**: `{{date}}`
- **Status**: `{{status}}`
- **Context**: `{{context}}`
- **Decision**: `{{decision}}`
- **Consequences**:
  - Positive: `{{positive}}`
  - Negative: `{{negative}}`
  - Neutral: `{{neutral}}`

### ADR-003: `{{title}}`

- **Date**: `{{date}}`
- **Status**: `{{status}}`
- **Context**: `{{context}}`
- **Decision**: `{{decision}}`
- **Consequences**:
  - Positive: `{{positive}}`
  - Negative: `{{negative}}`
  - Neutral: `{{neutral}}`

---

## 3. System Components

### 3.1 Component Map

| Component | Responsibility | Dependencies | Status |
|-----------|---------------|--------------|--------|
| `{{component}}` | `{{responsibility}}` | `{{dependencies}}` | `{{status}}` |

### 3.2 Component Interaction Diagram

```
{{interaction_diagram}}
```

### 3.3 Data Flow

```
{{data_flow}}
```

---

## 4. Patterns & Conventions

### 4.1 Design Patterns Used

| Pattern | Where Used | Rationale |
|---------|-----------|-----------|
| `{{pattern}}` | `{{location}}` | `{{rationale}}` |

### 4.2 Code Organization

| Convention | Description |
|-----------|-------------|
| `{{convention}}` | `{{description}}` |

### 4.3 API Design

| Style | Base URL | Auth |
|-------|----------|------|
| `{{style}}` | `{{base_url}}` | `{{auth}}` |

### 4.4 Data Models

| Model | Fields | Relationships |
|-------|--------|---------------|
| `{{model}}` | `{{fields}}` | `{{relationships}}` |

---

## 5. Cross-Cutting Concerns

### 5.1 Error Handling

| Error Type | Strategy | Logging |
|-----------|----------|---------|
| `{{type}}` | `{{strategy}}` | `{{logging}}` |

### 5.2 Security

| Concern | Implementation |
|---------|----------------|
| Authentication | `{{implementation}}` |
| Authorization | `{{implementation}}` |
| Data encryption | `{{implementation}}` |
| Input validation | `{{implementation}}` |

### 5.3 Observability

| Signal | Tool | Config |
|--------|------|--------|
| Logs | `{{tool}}` | `{{config}}` |
| Metrics | `{{tool}}` | `{{config}}` |
| Traces | `{{tool}}` | `{{config}}` |

### 5.4 Performance

| Strategy | Where Applied | Target |
|----------|--------------|--------|
| `{{strategy}}` | `{{where}}` | `{{target}}` |

---

## 6. Infrastructure

### 6.1 Deployment Architecture

```
{{deployment_diagram}}
```

### 6.2 Networking

| Network | Purpose | CIDR |
|---------|---------|------|
| `{{network}}` | `{{purpose}}` | `{{cidr}}` |

### 6.3 Storage

| Type | Location | Capacity | Backup |
|------|----------|----------|--------|
| `{{type}}` | `{{location}}` | `{{capacity}}` | `{{backup}}` |

---

## 7. Evolution Log

| Date | Change | ADR | Impact |
|------|--------|-----|--------|
| `{{date}}` | `{{change}}` | `{{adr}}` | `{{impact}}` |
