[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: Integration tests with real websites (use -m "not integration" to skip)
    slow: Slow tests requiring network access (use -m "not slow" to skip)
